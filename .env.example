# Copy to .env and set your own key. Never commit the real .env file.
OPENROUTER_API_KEY=replace_with_your_key

# Optional if WebArena Shopping Admin is not at the default local URL.
# WA_SHOPPING_ADMIN=http://localhost:7780/admin
# SHOPPING_ADMIN=http://localhost:7780/admin
