# Cycle 18A result and 18B bound-input effect protocol

Registered 2026-09-16 after complete public-code construction/regeneration and
before any new bound-program execution. Branch/HEAD and old evidence are unchanged.
Zero model calls and zero new external API USD are planned.

## Completed construction result

The [042 protocol](042_CYCLE18_PUBLIC_BINDING_PREREGISTRATION.md) is implemented
as a separate research prototype, leaving earlier frozen source operators intact.
The constructor accepts only exact original target/donor code, original proposal
spans and public action-prefix code. It has no task-ID, native-score, planner,
hidden namespace or intervention-outcome argument. The provenance adapter checks
the original source identity, then strips those metadata before construction.

Twenty focused fixtures pass, covering input/schema integrity, arbitrary symbols,
ambiguous/dynamic keyword arguments, capture, setup retention, output loss,
conditional binding uncertainty and unchanged target consumer AST. The complete
34-record screen regenerates exactly: **five accepted changed candidates, eight
accepted unchanged candidates and 21 explicit rejections**. Twenty rejections
involve unclosed external inputs; one involves lost required target outputs.
There are five distinct changed target-boundary/programs, all on b7a9ee9_1.

Primary construction coverage is one newly covered task; **KEEP for native effect
testing only**. This result proves neither that names are available at runtime
nor that the program's API fields, filtering, side effects or later continuation
are correct. Prefix-code definitions can have failed when originally executed.
No hidden state was used to fill an input, and no task-specific renaming table or
donor credential value was invented. Existing local donor code is not generalized
memory merely because a new input mapping can be inferred.

Root: `artifacts/research/cycle18_public_binding`.
Protocol: `a82023d42ff7adb9b949f9f41035888a564e861de3634cb96c727db9c15decf0`.
Report: `961c2829fc45d5d1c02e989dd454cbf248e65e1363d3480d3409b2ae87b8e9fe`.

## 18B fixed hypothesis and sample

Hypothesis: at least one of the five automatically bound programs repairs the
new task under the exact saved-continuation effect conditions. The strongest
alternatives are multiple independent source bugs, stale later actions, wrong
field/filter semantics, and false confidence from syntactic dependency closure.

Execute **all five distinct accepted changed programs**, in sorted
`(origin episode, action index, new program AST digest)` order. Do not inspect
partial outcomes to replace, rank or expand candidates. Preserve all source
provenance IDs; deduplicate only an exact target-boundary/program match.

First recollect one factual control per represented source episode, using its
earliest selected boundary (then minimum provenance digest). There are three
source episodes and therefore three controls. Each control and each intervention
uses one live worker and one fresh full-prefix replay, each scored separately:
**eight cells, sixteen native executions and sixteen native scorers**.
No model generation, new seed, deletion search, additional donor, manual repair,
new task or reserved-partition inspection is allowed by this protocol.

## Identical effect pathway and primary gate

Reuse the unchanged cycle-17 saved-continuation cell engine, original 50-action
image `sha256:b5f9aaedde041e30fc4ff795ab46d47849cfc72701b9ffc8c8983fdefe36fcf6`,
original source environment seeds and canonical 150-file public bundle. New
bound-candidate records must have their own schema and source/construction
digests; do not overwrite old proposals or fabricate a newly generated handoff.

Verify the original planner/public-handoff and complete supported pre-action
state. Replace exactly one action and replay every remaining saved action string
unchanged, even if now stale. Factual controls must match the original complete
source state/public output/completion/native score. Fresh effect replays must
match live state/public output/error positions/native score. Record tool-log counts,
measured wall time, all errors/exclusions and all zero-call accounting.

A passing edit requires factual source failure, new native success and completion,
supported exact replay, and no newly introduced uncaught error position relative
to the source. Unsupported final state is a retained exclusion; any other integrity
or infrastructure failure stops for explicit diagnosis, not a silent new attempt.

Primary metric: new task IDs other than the old aa8502b_1 task with at least one
passing edit. **KEEP the local bound-repair finding** if it is at least one and all
eight cells are accounted for; otherwise **REVISE**. Record partial native scores
only as descriptive outcomes, not as a substitute primary metric. A missing-name
error disappearing without native success is not a task repair.

These targets originally failed, so this still does not measure harmful flips on
successful origins. Even a positive local result is unadmitted and requires
independent same-rule support, successful-origin guards, learned verifier/scope,
static/text/equal-compute controls and group-disjoint transfer. Historical donor
policy/demo/cost differences and extra native tool work remain explicit.
