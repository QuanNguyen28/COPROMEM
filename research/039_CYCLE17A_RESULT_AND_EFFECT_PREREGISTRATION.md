# Cycle 17A result and 17B saved-continuation effect protocol

Registered 2026-09-16 before executing any expanded-boundary intervention.
Zero model calls; zero new external API USD. This is a build-only local repair
diagnostic, not a learned-memory treatment or a held-out method comparison.

## Completed coverage result

The complete 32-source registry supplies six eligible same-task contrasts.
The all-boundary screen examines all 1,323 action pairs: 1,188 no-proposal pairs,
63 unsupported-syntax pairs, 42 error-producing donor actions, and 30 pairs with
proposals. There are 34 proposal provenance records and 26 distinct
`(target episode, target action index, replacement program AST)` interventions.
Eight records duplicate a program on the same target boundary; none count as
independent support. All records and the report regenerate from frozen sources.

The old aa8502b task supplies 28 proposal records; the new b7a9ee9 task supplies
six. Primary metric is one newly covered task, hence **KEEP the alignment expansion
for effect testing only** under [038](038_CYCLE17_ALL_BOUNDARY_SCREEN_PREREGISTRATION.md).
There is still no new validated repair, learned predicate, scope or admitted bank.

Screen report: `47ba12900b34835c813f99dd689b7d01b75c81be5e7ec559dfae3ea1b7778268`.
Protocol: `a459726149f12360547116faf9e3098f0f5539dd077531abe41db5cf9331fe03`.
Store: `artifacts/research/cycle17_all_boundary_screen`.

## 17B hypothesis, fixed selection and controls

Hypothesis: at least one proposed block causes a newly covered failed source task
to succeed when substituted at its exact recorded boundary, with all earlier
state/planner evidence and subsequent saved programs fixed. Accidental identifier
matches, missing donor namespace, stale later actions and irrelevant procedural
changes are strong falsifying alternatives.

Execute **all 26 distinct interventions**, with no quality-based selection or
replacement. Group the 34 candidates by the exact episode/action/AST triple,
retain every provenance ID, and select its lexicographically smallest candidate
digest as the executable representative. Execute representatives by sorted triple.
Before interventions, execute one factual full-sequence control for every target
episode represented, using its earliest candidate boundary (then smallest digest)
for the pre-action check. Every control and intervention receives one live run
plus one fresh full-prefix replay and two separate unmodified native scorers.
No additional seeds, model calls, manual repairs or deletion search occur in 17B.

The pre-run manifest must derive exact counts from the complete screen, freeze all
candidate IDs and implementation sources, and refuse more than 32 interventions.
If fewer than 26 distinct interventions regenerate, stop for diagnosis instead of
silently changing the sample. Expected native executions/scorers equal twice
`(26 + number of target source episodes)`; record this exact count before execution.

## Fixed checkpoint and continuation

Use the original 50-action image
`sha256:b5f9aaedde041e30fc4ff795ab46d47849cfc72701b9ffc8c8983fdefe36fcf6`,
the cycle-15 canonical 150-file public bundle and the original target's environment
seed. The official source contributes only a recorded donor program, not its
different model/runtime. Its historical cost and human demonstration remain
disclosed. Hidden scorer/task-ground-truth data never enters the worker.

Reconstruct the entire original action prefix in an isolated live worker and
verify it against the saved full supported state, public outputs and exact target
planner/public-handoff binding. Substitute only the one selected action string.
Then execute every remaining original action string, in order, unchanged. Never
regenerate the planner or executor. Preserve saved later actions even when their
old observations are now stale. This deliberate **open-loop saved-continuation**
estimand tests local program effect, not performance of an adaptive recovery policy.

Factual controls must reproduce the original final supported state, public
observations, completion and native score. Every effect's independent replay must
match the live result/state and native score. Record native API-log entry counts,
total/prefix/suffix errors, completion, wall time and zero model/API accounting.
API work is not forced equal; any gain with extra tool calls is not an efficiency
claim or a fair final method win.

## Primary outcome and decision

A candidate passes only when the factual source fails, the substituted sequence
is native-successful and completed, supported final state and exact replay pass,
and it introduces no uncaught action-error position relative to that factual
sequence. Old errors may disappear; every observed/introduced error is retained.
Unsupported final namespaces are explicit exclusions; all other integrity or
infrastructure failures stop for diagnosis with raw partial evidence preserved.

Primary metric: new task IDs other than old aa8502b_1 with at least one passing
candidate. **KEEP the local repair finding** only if this is at least one and all
registered controls/interventions are accounted for. Otherwise **REVISE**. Report
all per-candidate outcomes and partial native-score changes without selecting on
them. This exploratory search's positive rate is not a held-out confidence estimate.

All target source episodes are failures, so this experiment cannot estimate
harmful flips on previously successful episodes. That guard, independent support
for the same abstract rule, learned executable verifier/scope, minimization,
static/text/equal-compute controls and group-disjoint transfer must precede any
contract admission or method-level claim. Do not call two unrelated local repairs
two independent supports for one contract. No final-test task is opened here.
